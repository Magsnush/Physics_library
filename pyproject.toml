[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "small_x_physics"
version = "0.1.0"
description = "Reusable cross section physics building blocks"
dependencies = [
    "numpy",
    "scipy"
]