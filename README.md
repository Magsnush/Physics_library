# physicslib

Reusable cross-section physics building blocks used for photon–proton calculations.

This repository provides implementations of lightcone wavefunctions, multipole correlators, and integrands intended for numerical integration and analysis.

Quick start
1. Create and activate a Python environment (recommended).
2. Install minimal runtime dependencies:

```bash
pip install -e .
```

